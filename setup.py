from setuptools import setup

setup(
    name='Array Addition',
    version='0.1',
    description='Performs array addition operation',
    author='',
    author_email='',
    install_requires=[
        'numpy',
      ],
)
